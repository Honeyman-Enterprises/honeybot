# Copy to .env.local for local development overrides (.env.local is gitignored).
# Most developers will only set OP_SERVICE_ACCOUNT_TOKEN here and let Varlock
# resolve the rest from 1Password.

# OP_SERVICE_ACCOUNT_TOKEN=ops_...

# Override which 1Password vault to use while testing:
# APP_ENV=dev
